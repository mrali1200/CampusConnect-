// Polyfill for crypto.getRandomValues()
import 'react-native-get-random-values';

// Import this file at the top of your app's entry point (usually App.tsx or index.js)
// to ensure the polyfill is loaded before any other code runs.
